{
  "name": "team-cop-agent",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "test": "node --test",
    "test:runtime": "node --test ../runtime-tests/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "miniflare": "5.20260921.0-alpha",
    "wrangler": "^4.137.0"
  },
  "dependencies": {
    "@shortcut/client": "^3.4.1"
  }
}
